// KMP!!

class Solution {
public:
    int repeatedStringMatch(string A, string B) {
    	int n = A.length(), m = B.length();

    	if (m==0)
    		return 1;
    	if (n==0)
    		return -1;

    		
    }
};